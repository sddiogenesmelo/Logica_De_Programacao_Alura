/*  Crie um contador que comece em 1 e vá até 10 usando um loop while. Mostre cada número.

    Crie um contador que começa em 10 e vá até 0 usando um loop while. Mostre cada número.

    Crie um programa de contagem regressiva. Peça um número e conte deste número até 0,
    usando um loop while no console do navegador.

    Crie um programa de contagem progressiva. Peça um número e conte de 0 até esse número, 
    usando um loop while no console do navegador.*/

// Contador progressivo. (OK)

/*let nome = prompt('Digite seu nome: ');
    alert(`Seja bem vindo ${nome}.`);

let contador = 1;

while (contador <= 10){
    alert(contador);

    contador++;
} 

//contador regressivo. (OK)

let nome = prompt('Digite seu nome: ');
    alert(`Seja bem vindo ${nome}.`);

let contador = 10;

while (contador >= 0) {
    alert(contador);

    contador--;
}

//contagem regressiva com prompt. (ok)

let nome = prompt('Digite seu nome: ');
    alert(`Seja em vindo ${nome}.`);

alert('Esse desafio consiste em você adicionar um número e o app vai subtraindo até chegar em 0.');

let numeroSugerido = prompt('Digite um número: ');

while (numeroSugerido == 0){
    alert('Número precisa ser maior do que 0 ! ' );
    numeroSugerido = prompt('Digite um numero maior que 0: ')
}

alert('OK! Vamos começar.')

let contador = numeroSugerido;

while (contador >= 0){
    alert(contador);

    contador--;
}

// Contagem progressiva com prompt

let nome = prompt('Digite seu nome: ');
    alert(`Seja em vindo ${nome}.`);

alert('Esse desafio consiste em você adicionar um número e o app vai somando até chegar no número sugerido.');

let numeroSugerido = prompt('Digite um número: ');

while (numeroSugerido == 0){
    alert('Número precisa ser maior do que 0 ! ' );
    numeroSugerido = prompt('Digite um numero maior que 0: ')
}

alert(`OK! Vamos começar ${nome}. `);
alert(`O seu numero é: ${numeroSugerido}`)

let contador = 1;

while (contador != numeroSugerido){
    alert(contador);
    contador++;
}*/